#include "cpuMLFQ.h"




int main() {

	MLFQ a;
	a.start();








	//shared_ptr<queue> queue1 =make_shared<queue>();
	//shared_ptr<queue> queue2 = make_shared<queue>();
	//shared_ptr<queue> fcfs = make_shared<queue>();
	////should be a list instead
	//shared_ptr<stack> IO = make_shared<stack>();

	//init(queue1);


	/*thread t1(test1, a);
	cout << a->empty() << endl;
	while (!a->empty())
	{
		thread t2(test2, a);
		thread t3(test3, a);
		t3.join();
		t2.join();
	}
	t1.join();*/

	return 0;
}